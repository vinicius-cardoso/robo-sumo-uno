# Tests

Resultados e evidencias de teste.

- `logs/`: logs temporarios de sensores, motores e bateria.

Logs sao ignorados por padrao no Git para evitar crescimento desnecessario do
repositorio. Se algum log for importante para documentar uma decisao, mova um
resumo para `docs/` ou adicione um arquivo pequeno e nomeado explicitamente.

